/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/cse-specialization-app', // Replace with your repository name
};

export default nextConfig;
